=== shen-derive: Payment Demo Derivation Transcript ===

--- Step 0: Naive specification ---

  processable b0 txs = all (>= 0) (scanl apply b0 txs)
  where apply b tx = b - tx
        all p xs = foldr (\x acc -> p x && acc) True xs

  Verification: processable 100 [30,50,40] = false ✓
  Verification: processable 100 [30,20,10] = true ✓

--- Step 1: Express as composition ---

  processable b0 txs = (all_geq0 . scanl_apply b0) txs
  where all_geq0 = foldr (\x acc -> x>=0 && acc) True
        scanl_apply b0 = scanl apply b0

  AST: foldr (\x acc -> (\(x : Int) -> x >= 0) x && acc) True . scanl (\(b : Int) (tx : Int) -> b - tx) b0

--- Step 2: Fuse all + scanl into single fold ---

  By fold-fusion (Bird, Algebra of Programming, §3.1):
  all (>=0) . scanl apply b0
  = snd . foldl step (b0, True)
  where step (bal, ok) tx = let bal' = bal - tx
                            in (bal', ok && bal' >= 0)

  Verification: processable_fused 100 [30,50,40] = false ✓
  Verification: processable_fused 100 [30,20,10] = true ✓

--- Step 3: Discharge side conditions ---

  Side condition: all_geq0 distributes over scanl_apply step: check(bal-tx) && ok = snd(step((bal,ok),tx))
  Empirical check: passed (verified on 25 test cases)
  Shen validation: emitted spec accepted by tc+ (validation only; not a proof)

--- Step 4: Lower to Go ---

  Generated Go function:

    // Code generated by shen-derive. DO NOT EDIT.
    //
    // Derived from: processable b0 txs = all (>= 0) (scanl apply b0 txs)
    // Via: scanl-fold fusion (Bird, Algebra of Programming, §3.1)
    // Side conditions checked empirically; emitted Shen validated with tc+.
    
    package derived
    
    // Processable checks whether all running balances remain non-negative
    // when applying a sequence of transaction amounts to an initial balance.
    //
    // Derived from the naive specification:
    //   processable b0 txs = all (>= 0) (scanl (\b tx -> b - tx) b0 txs)
    //
    // Fused into a single-pass fold with O(1) space:
    //   processable b0 txs = snd (foldl step (b0, True) txs)
    //   where step (bal, ok) tx = let bal' = bal - tx in (bal', ok && bal' >= 0)
    func Processable(b0 int, txs []int) bool {
    	bal := b0
    	ok := true
    	for _, tx := range txs {
    		bal = bal - tx
    		ok = ok && bal >= 0
    	}
    	return ok
    }
    

--- Step 5: Verify equivalence ---

  Naive vs fused equivalence verified on test cases ✓
  Generated Go compiles and matches fused algorithm ✓

=== Derivation chain summary ===

  1. SPEC:    processable b0 txs = all (>=0) (scanl apply b0 txs)
  2. REWRITE: scanl-fold fusion → snd . foldl step (b0, True)
     RULE:    fold-fusion (Bird, Algebra of Programming, §3.1)
     SIDE:    commutativity of (&&) — empirical check + Shen tc+ validation
  3. LOWER:   foldl → forward for-loop, tuple → two variables
  4. OUTPUT:  Processable(b0 int, txs []int) bool

The efficient Go implementation is equivalent to the naive specification.
Each step is justified by a named algebraic law with recorded side-condition checks.
