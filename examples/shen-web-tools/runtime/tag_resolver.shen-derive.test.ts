// Code generated by shen-derive-ts. DO NOT EDIT.
//
// This file checks that ResolveTagBlockChildren matches the Shen spec
// by evaluating the spec on sampled inputs and comparing outputs.

import { test } from "node:test";
import assert from "node:assert/strict";
import * as shenguard from "./guards_gen.js";
import { ResolveTagBlockChildren } from "./tag_resolver.js";

// helpers referenced: mustPartial, mustRefTable, mustRefTableEntry, mustTagBlock, mustTagId, mustTagSignature, mustUnsignedComplete

test("case_00", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("root-signed"), "Signed root", [shenguard.mustTagId("child-a")], shenguard.mustTagSignature("sig-root")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("child-a"), shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("root-signed"), "Signed root", [shenguard.mustTagId("child-a")], shenguard.mustTagSignature("sig-root")), [shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature(""))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_01", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("root-unsigned"), "Unsigned root", [shenguard.mustTagId("child-a")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("child-a"), shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("root-unsigned"), "Unsigned root", [shenguard.mustTagId("child-a")], shenguard.mustTagSignature("")), [shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature(""))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_02", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("root-partial"), "Partial root", [shenguard.mustTagId("child-a"), shenguard.mustTagId("missing-child")], shenguard.mustTagSignature("sig-root")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("child-a"), shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("root-partial"), "Partial root", [shenguard.mustTagId("child-a"), shenguard.mustTagId("missing-child")], shenguard.mustTagSignature("sig-root")), [shenguard.mustTagBlock(shenguard.mustTagId("child-a"), "Child A", [], shenguard.mustTagSignature(""))], [shenguard.mustTagId("missing-child")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_03", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_04", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_05", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_06", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_07", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_08", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_09", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_10", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))), shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))), shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")), []);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_11", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_12", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_13", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_14", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_15", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_16", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_17", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("alice")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_18", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))), shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))), shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")), [shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_19", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_20", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_21", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_22", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_23", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_24", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_25", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("bob")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_26", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))), shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))), shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")), [shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_27", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_28", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_29", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_30", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob"))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_31", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_32", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_33", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [], [shenguard.mustTagId(" ")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_34", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))), shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))), shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")), [shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob"))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_35", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_36", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_37", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_38", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_39", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature(""))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_40", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_41", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_42", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature(""))), shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice"))), shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")), [], [shenguard.mustTagId("A")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_43", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_44", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("alice"), shenguard.mustTagBlock(shenguard.mustTagId("alice"), "", [], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_45", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("bob"), shenguard.mustTagBlock(shenguard.mustTagId("bob"), "alice", [shenguard.mustTagId("alice")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_46", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId(" "), shenguard.mustTagBlock(shenguard.mustTagId(" "), "bob", [shenguard.mustTagId("bob")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_47", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("A"), shenguard.mustTagBlock(shenguard.mustTagId("A"), "", [shenguard.mustTagId(" ")], shenguard.mustTagSignature("")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_48", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice")))]));
  const want = shenguard.mustUnsignedComplete("unsigned-complete", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "alice", [shenguard.mustTagId("A")], shenguard.mustTagSignature("alice"))]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
test("case_49", () => {
  const got = ResolveTagBlockChildren(shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), shenguard.mustRefTable([shenguard.mustRefTableEntry(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")))]));
  const want = shenguard.mustPartial("partial", shenguard.mustTagBlock(shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"), "bob", [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")], shenguard.mustTagSignature("bob")), [], [shenguard.mustTagId("AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA")]);
  assert.deepStrictEqual(got, want, `resolve-tag-block-children: spec says ${JSON.stringify(want)}, impl returned ${JSON.stringify(got)}`);
});
