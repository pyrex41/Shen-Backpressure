\* Shen sequent-calculus type specifications *\
\* Generated by sb init — edit this file to define your domain invariants *\

\* Wrapper: domain-specific string, no validation *\
(datatype item-id
  X : string;
  ==============
  X : item-id;)

\* Constrained: validated numeric value *\
(datatype amount
  X : number;
  (>= X 0) : verified;
  ====================
  X : amount;)

\* Composite: structured type with typed fields *\
(datatype line-item
  Id : item-id;
  Qty : amount;
  ===========================
  [Id Qty] : line-item;)

\* Example of decidable-fragment tagging (for the native-Shen terminating
   runtime policy tier parallel to Cedar). The judgment (sequent calculus
   + Prolog) + tiny emitter certifies no general recursion + stratified
   Horn bodies. (See plan + shen-cedar for lattice.)
\*
\* @decidable-fragment: example-policy-predicate
(datatype example-policy-predicate
  X : string;
  (= X "allow") : verified;
  ===========================
  X : example-policy-predicate;)
